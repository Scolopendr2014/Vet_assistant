# Быстрый старт: Запуск GitLab

**Если у вас 8 ГБ ОЗУ и GitLab не запускается** — см. [docs/ВАРИАНТЫ_КОНТРОЛЯ_ВЕРСИЙ.md](docs/ВАРИАНТЫ_КОНТРОЛЯ_ВЕРСИЙ.md): там описаны лёгкие варианты (Git + GitHub/GitLab.com, Gitea в Docker).

## ✅ Шаг 1: Docker Desktop установлен

Docker Desktop успешно установлен на вашем компьютере.

## 🔄 Шаг 2: Запустите Docker Desktop

1. **Найдите Docker Desktop** в меню "Пуск" и запустите его
2. **Дождитесь полной загрузки** (иконка в трее станет зелёной)
   - Это может занять 1-2 минуты при первом запуске
3. **Примите условия лицензии** (если появится запрос)

## 🚀 Шаг 3: Запустите GitLab

После того, как Docker Desktop запустится, выполните в PowerShell:

```powershell
# Перейдите в папку проекта
cd "C:\Users\User\Desktop\Новая папка\Асистент ветеринара"

# Запустите GitLab
docker-compose up -d
```

**Или используйте автоматический скрипт:**

```powershell
.\gitlab\docker-setup.ps1
```

## ⏳ Шаг 4: Дождитесь запуска GitLab

GitLab запускается **5-10 минут** при первом запуске (скачивается образ ~1.5 ГБ).

Проверить статус:
```powershell
docker ps
```

Просмотреть логи:
```powershell
docker logs -f gitlab
```

## 🔑 Шаг 5: Получите пароль

После запуска получите начальный пароль root:

```powershell
docker exec -it gitlab grep 'Password:' /etc/gitlab/initial_root_password
```

## 🌐 Шаг 6: Откройте GitLab

1. Откройте браузер
2. Перейдите на: **http://localhost**
3. Войдите:
   - **Логин:** `root`
   - **Пароль:** (из команды выше)

## ⚠️ Важно

- **Требования к памяти:** GitLab требует минимум 4 ГБ RAM (рекомендуется 6–8 ГБ).
- **Настройка памяти Docker (Windows):**
  - В режиме **WSL 2** (по умолчанию) в Settings нет пункта Memory — память настраивается через файл **.wslconfig**. Подробно: [docs/DOCKER_WSL2_ПАМЯТЬ.md](docs/DOCKER_WSL2_ПАМЯТЬ.md).
  - Если используется **Hyper-V**, то: Settings → Resources → Advanced → **Memory** → минимум 6 ГБ.

## 📝 Следующие шаги

После входа в GitLab:

1. **Измените пароль root:**
   - Settings → Account → Change password

2. **Создайте проект:**
   - New project → Create blank project
   - Название: `vet-assistant`

3. **Подключите локальный репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin http://localhost/root/vet-assistant.git
   git push -u origin main
   ```

## 🛠️ Полезные команды

```powershell
# Остановить GitLab
docker-compose stop

# Запустить GitLab
docker-compose start

# Перезапустить GitLab
docker-compose restart

# Остановить и удалить (ОСТОРОЖНО: удалит все данные!)
docker-compose down

# Просмотреть логи
docker logs -f gitlab
```

## ❓ Проблемы?

### Пустая страница при открытии http://localhost

Чаще всего GitLab ещё **не успел полностью запуститься** (первый запуск — 10–15 минут).

1. **Проверьте, что контейнер запущен:**
   ```powershell
   docker ps
   ```
   Должен быть контейнер `gitlab` со статусом `Up` (желательно несколько минут).

2. **Посмотрите логи** — в конце должно быть что-то вроде «GitLab is ready»:
   ```powershell
   docker logs --tail 50 gitlab
   ```
   Если видите перезапуски или ошибки памяти — подождите ещё или увеличьте память в Docker Desktop (Settings → Resources → Memory → 4–6 ГБ).

3. **Подождите 10–15 минут** после первого `docker-compose up -d`, затем обновите страницу (Ctrl+F5).

4. **Проверьте порт 80** — не занят ли другим приложением:
   ```powershell
   netstat -ano | findstr :80
   ```
   Если порт занят, в `docker-compose.yml` измените порт на `8080:80` и откройте http://localhost:8080

### STATUS unhealthy в `docker ps`

Обычно это значит, что GitLab ещё **запускается** (10–15 минут) или **не хватает памяти**.

1. **Подождите 10–15 минут** — в `docker-compose.yml` добавлен `start_period: 900s`, после перезапуска контейнер не будет помечаться как unhealthy во время старта. Перезапустите:
   ```powershell
   docker-compose down
   docker-compose up -d
   ```
   и подождите 15 минут.

2. **Проверьте логи** на ошибки и OOM (нехватка памяти):
   ```powershell
   docker logs --tail 100 gitlab
   ```

3. **Увеличьте память для Docker:** в WSL 2 — через файл **.wslconfig** (см. [docs/DOCKER_WSL2_ПАМЯТЬ.md](docs/DOCKER_WSL2_ПАМЯТЬ.md)); в Hyper-V — Settings → Resources → Advanced → Memory → **6–8 ГБ**. Затем перезапустите контейнер:
   ```powershell
   docker-compose restart
   ```

4. Если через 15+ минут статус всё ещё unhealthy — смотрите логи и убедитесь, что порт в браузере совпадает с `docker-compose.yml` (например http://localhost:8080 при `8080:80`).

5. **Если в логах идёт "database_migrations" / "db:schema:load" / "NOTICE: extension ... already exists"** — это **нормально**. Идёт первый запуск и миграции БД; они могут занять **ещё 10–30 минут**. Статус unhealthy в это время ожидаем. Просто подождите и смотрите логи (`docker logs -f gitlab`) — успех: строка «GitLab is ready» или «gitlab Reconfigured».

### Ошибка 500 при открытии http://localhost:8080 ("We're sorry, something went wrong")

Частая причина — **неверный external_url**: в конфиге было `http://localhost` (порт 80), а вы заходите по **:8080**. В `docker-compose.yml` должно быть `external_url 'http://localhost:8080'` (уже исправлено).

**Что сделать:** применить новый конфиг и перезапустить GitLab:

```powershell
cd "C:\Users\User\Desktop\Новая папка\Асистент ветеринара"
docker-compose down
docker-compose up -d
```

Подождите 2–3 минуты, затем выполните **reconfigure** внутри контейнера (чтобы GitLab перечитал external_url):

```powershell
docker exec gitlab gitlab-ctl reconfigure
```

Подождите ещё 1–2 минуты и откройте снова **http://localhost:8080**.

**«Зацикленные» логи:** если в логах каждые 30 секунд повторяется одно и то же (например, проверка здоровья) — это **нормально** (healthcheck). Если повторяются **ошибки** — пришлите последние 20–30 строк логов.

### Ошибки PostgreSQL (база не создаётся / не запускается)

К этой группе относятся сообщения в логах:

- **"Exhausted service checks and database is still not available"**
- **"Error executing action `create` on resource 'database_objects[postgresql]'"**
- **"base/1 is not a valid data directory"** или **"File base/1/PG_VERSION is missing"**

Они означают, что **PostgreSQL внутри контейнера не смог запуститься или создать объекты БД** — из‑за **нехватки памяти** или **повреждённых данных** в папке `gitlab/data` (например, после прерванного или неудачного старта). Такой каталог данных **починить нельзя** — нужен только **чистый старт** (удалить данные и запустить заново).

**Что сделать по шагам:**

1. **Выделить больше памяти для Docker.** В режиме WSL 2 пункта Memory в Settings нет — настройте память через файл **.wslconfig** (см. [docs/DOCKER_WSL2_ПАМЯТЬ.md](docs/DOCKER_WSL2_ПАМЯТЬ.md)). Рекомендуется **6–8 ГБ** для WSL 2.

2. **Остановить контейнер и удалить старые данные** (чистый старт):
   ```powershell
   cd "C:\Users\User\Desktop\Новая папка\Асистент ветеринара"
   docker-compose down
   ```
   Удалите папки с данными (если проектов в GitLab ещё нет — смело удаляйте):
   ```powershell
   Remove-Item -Recurse -Force .\gitlab\data
   Remove-Item -Recurse -Force .\gitlab\config
   Remove-Item -Recurse -Force .\gitlab\logs
   ```
   Создайте пустые папки снова:
   ```powershell
   New-Item -ItemType Directory -Force .\gitlab\data, .\gitlab\config, .\gitlab\logs
   ```

3. **Запустить заново** и подождать 15–20 минут:
   ```powershell
   docker-compose up -d
   docker logs -f gitlab
   ```
   В логах дождитесь строки вроде «GitLab is ready» или «gitlab Reconfigured».

В `docker-compose.yml` уже выставлены лимит памяти 6 ГБ и `shm_size: 512m` — этого должно хватить при 6–8 ГБ у Docker Desktop.

**Перед чистым стартом:** закройте лишние приложения, чтобы освободить RAM. Если ошибки PostgreSQL повторяются — выделите для WSL 2 **8 ГБ** в `.wslconfig` (см. [docs/DOCKER_WSL2_ПАМЯТЬ.md](docs/DOCKER_WSL2_ПАМЯТЬ.md)).

**Если ошибка «Exhausted service checks» возвращается после чистого старта** — почти всегда не хватает памяти. Проверьте: в `.wslconfig` указано `memory=8GB`, выполнено `wsl --shutdown`, затем заново запущен Docker Desktop и только после этого `docker-compose up -d`.

### GitLab вообще не запускается

1. **Проверьте, что Docker Desktop запущен**
2. **Проверьте логи:** `docker logs gitlab`
3. **Убедитесь, что порт 80 свободен:**
   ```powershell
   netstat -ano | findstr :80
   ```
4. **Если порт занят**, измените порт в `docker-compose.yml`:
   ```yaml
   ports:
     - '8080:80'  # Используйте порт 8080
   ```
   Тогда GitLab будет доступен на http://localhost:8080

---

**Готово!** После выполнения этих шагов GitLab будет работать локально на вашем компьютере.
