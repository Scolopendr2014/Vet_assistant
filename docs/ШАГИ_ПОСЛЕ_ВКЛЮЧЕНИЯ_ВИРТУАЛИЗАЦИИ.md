# Шаги после включения виртуализации в BIOS

## ✅ Шаг 1: Проверка Docker

После включения виртуализации в BIOS и перезагрузки компьютера, проверьте, что Docker Desktop работает корректно.

### Проверка в PowerShell:

```powershell
# 1. Проверьте версию Docker
docker --version

# 2. Проверьте версию Docker Compose
docker-compose --version

# 3. Проверьте запущенные контейнеры
docker ps

# 4. Запустите тестовый контейнер (проверка виртуализации)
docker run --rm hello-world
```

**Ожидаемый результат:**
- Все команды выполняются без ошибок
- Тестовый контейнер `hello-world` запускается и выводит приветственное сообщение
- Если всё работает - виртуализация включена правильно! ✅

---

## 🚀 Шаг 2: Настройка Docker Desktop (если нужно)

Если Docker Desktop ещё не настроен:

1. **Откройте Docker Desktop**
2. **Настройте ресурсы:**
   - Settings → Resources → Memory
   - Установите минимум **4 ГБ** (рекомендуется 6-8 ГБ для GitLab)
   - Settings → Resources → CPUs
   - Выделите минимум **2 CPU** (рекомендуется 4)

3. **Убедитесь, что Docker запущен:**
   - Иконка Docker в трее должна быть зелёной
   - В Docker Desktop должно быть написано "Docker Desktop is running"

---

## 📦 Шаг 3: Запуск GitLab (опционально)

Если вы хотите использовать локальный GitLab для хранения кода:

### Вариант A: Автоматический запуск (рекомендуется)

```powershell
# Перейдите в папку проекта
cd "C:\Users\User\Desktop\Новая папка\Асистент ветеринара"

# Запустите скрипт установки
.\gitlab\docker-setup.ps1
```

### Вариант B: Ручной запуск

```powershell
# Перейдите в папку проекта
cd "C:\Users\User\Desktop\Новая папка\Асистент ветеринара"

# Создайте папки для GitLab (если их нет)
New-Item -ItemType Directory -Path "gitlab\config" -Force
New-Item -ItemType Directory -Path "gitlab\logs" -Force
New-Item -ItemType Directory -Path "gitlab\data" -Force

# Запустите GitLab
docker-compose up -d
```

**Ожидание:** GitLab запускается 5-10 минут при первом запуске (скачивается образ ~1.5 ГБ).

### Проверка статуса GitLab:

```powershell
# Проверьте, что контейнер запущен
docker ps

# Просмотрите логи (для отслеживания прогресса)
docker logs -f gitlab
```

### Получение пароля root:

```powershell
# Получите начальный пароль (подождите 2-3 минуты после запуска)
docker exec -it gitlab grep 'Password:' /etc/gitlab/initial_root_password
```

### Открытие GitLab:

1. Откройте браузер
2. Перейдите на: **http://localhost**
3. Войдите:
   - **Логин:** `root`
   - **Пароль:** (из команды выше)

---

## 🌐 Альтернатива: Использование GitLab.com

Если не хотите устанавливать локальный GitLab, используйте облачный GitLab.com:

1. **Зарегистрируйтесь:** https://gitlab.com/users/sign_up
2. **Создайте проект:** New project → Create blank project
3. **Подключите репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://gitlab.com/ваш-username/vet-assistant.git
   git push -u origin main
   ```

**Преимущества:**
- ✅ Не требует установки
- ✅ Работает сразу
- ✅ Бесплатный план для личных проектов
- ✅ Автоматические обновления

---

## 🔧 Шаг 4: Настройка Git (если ещё не сделано)

Если Git ещё не настроен, следуйте инструкциям в:
- `docs\НАСТРОЙКА_GIT.md`

---

## 📝 Следующие шаги разработки

После настройки Docker и GitLab:

1. **Инициализируйте Git репозиторий** (если ещё не сделано)
2. **Создайте первый коммит**
3. **Начните разработку** согласно техническому заданию:
   - См. `docs\СТАТУС_ПРОЕКТА.md` - что уже сделано
   - См. `docs\ТЗ_Техническое_Задание.md` - что нужно делать дальше

---

## ❓ Устранение проблем

### Docker не запускается

1. **Проверьте, что виртуализация включена:**
   - Откройте Диспетчер задач → Производительность → CPU
   - Должно быть написано "Виртуализация: Включена"

2. **Перезапустите Docker Desktop:**
   - Правый клик на иконку Docker → Quit Docker Desktop
   - Запустите снова

3. **Проверьте WSL2 (если используется):**
   ```powershell
   wsl --status
   ```

### GitLab не запускается

1. **Проверьте логи:**
   ```powershell
   docker logs gitlab
   ```

2. **Проверьте использование ресурсов:**
   ```powershell
   docker stats
   ```

3. **Убедитесь, что порт 80 свободен:**
   ```powershell
   netstat -ano | findstr :80
   ```

4. **Если порт занят**, измените порт в `docker-compose.yml`:
   ```yaml
   ports:
     - '8080:80'  # Используйте порт 8080
   ```
   Тогда GitLab будет доступен на http://localhost:8080

### Недостаточно памяти

- Увеличьте выделенную память для Docker Desktop:
  - Settings → Resources → Memory → минимум 4 ГБ (для GitLab)

---

## ✅ Чек-лист готовности

- [ ] Docker Desktop установлен и запущен
- [ ] Виртуализация включена в BIOS
- [ ] Команда `docker --version` работает
- [ ] Тестовый контейнер `docker run --rm hello-world` запускается
- [ ] (Опционально) GitLab запущен и доступен на http://localhost
- [ ] (Опционально) Git настроен и репозиторий подключён

**Если все пункты отмечены - вы готовы к разработке!** 🎉

---

**Дата создания:** 2026-01-28  
**Следующий шаг:** Начните разработку согласно `docs\СТАТУС_ПРОЕКТА.md`
