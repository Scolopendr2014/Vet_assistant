# Статус проекта "Ассистент ветеринара"

**Дата обновления**: 2026-02-04  
**Версия**: 1.0.0 (MVP в разработке, пациенты и протоколы работают)

---

## ✅ Что уже реализовано

### 1. Документация
- ✅ **Бизнес-требования** (`docs/ТЗ_Бизнес_Требования.md`) - полное описание для не-технических специалистов
- ✅ **Техническое задание** (`docs/ТЗ_Техническое_Задание.md`) - детальное ТЗ для разработчиков
- ✅ **Архитектура** - спроектирована модульная структура с Clean Architecture

### 2. Структура проекта
- ✅ Создан Flutter проект с модульной структурой
- ✅ Настроен `pubspec.yaml` со всеми необходимыми зависимостями
- ✅ Создана базовая структура папок:
  - `lib/core/` - общие утилиты, конфигурация, DI, БД
  - `lib/features/` - модули приложения (patients, templates, examinations, speech, pdf, admin)

### 3. База данных
- ✅ Схема БД (Drift/SQLite):
  - Таблица `patients` (пациенты)
  - Таблица `examinations` (осмотры)
  - Таблица `templates` (шаблоны протоколов)
  - Таблица `references` (справочники)
  - Индексы для оптимизации поиска

### 4. Модуль распознавания речи (STT)
- ✅ Абстракция `SpeechRecognizer` для всех провайдеров
- ✅ Реализованы провайдеры (заглушки):
  - `CloudRecognizer` (Google/Yandex/Azure)
  - `PrivateServerRecognizer` (приватный сервер)
  - `OnDeviceRecognizer` (офлайн Whisper/Vosk)
- ✅ **SttRouter** - роутер для автоматического выбора провайдера:
  - Режимы: Auto, OfflineOnly, CloudOnly, PrivateOnly
  - Автоматический fallback на офлайн при отсутствии интернета
  - Проверка доступности провайдеров

### 5. JSON шаблоны протоколов
- ✅ **Кардиология** (`assets/templates/cardio.json`):
  - Основные показатели (ЧСС, ритм, тоны, шумы, АД)
  - Данные ЭКГ (интервалы, ритм)
  - Заключение и рекомендации
  - Правила извлечения полей из текста
  - Правила валидации
  
- ✅ **УЗИ** (`assets/templates/ultrasound.json`):
  - Печень, почки, мочевой пузырь, сердце
  - Параметры по каждому органу
  - Заключение
  
- ✅ **Стоматология** (`assets/templates/dental.json`):
  - Общий осмотр (прикус, гингивит, камень, налёт)
  - Карта зубов (таблица статусов)
  - Пародонтальные показатели
  - Заключение и план лечения

### 6. Навигация
- ✅ Настроен `go_router` с маршрутами:
  - Список пациентов
  - Детали пациента
  - Создание протокола
  - Детали протокола
  - Административный вход
  - Панель администратора

### 7. Конфигурация
- ✅ `AppConfig` - централизованная конфигурация:
  - Лимиты бесплатной версии
  - Настройки STT (таймауты, пороги confidence)
  - Пути к файлам
  - Настройки PDF

### 8. Модуль пациентов (реализован)
- ✅ Репозиторий для работы с БД (PatientRepository)
- ✅ CRUD: создание, чтение, обновление, удаление
- ✅ Поиск по кличке, номеру чипа, ФИО владельца
- ✅ UI: список пациентов, детали пациента, форма создания/редактирования
- ✅ Лимит 10 пациентов в бесплатной версии
- ✅ Просмотр истории осмотров по пациенту

### 9. Система шаблонов и модуль осмотров (реализованы)
- ✅ Загрузка JSON шаблонов из assets, кэширование в БД
- ✅ Генерация UI форм из шаблона (TemplateFormBuilder)
- ✅ Создание протокола: выбор пациента и типа (кардиология, УЗИ, стоматология)
- ✅ Запись анамнеза вручную, ручное редактирование полей по шаблону
- ✅ Валидация данных (обязательные поля)
- ✅ Сохранение протокола в БД
- ✅ Генерация PDF протокола (без фото)
- ✅ Просмотр, сохранение PDF, шаринг

### 10. Экспорт БД
- ✅ Экспорт пациентов и протоколов в JSON (без медиа в архиве)

### 11. Административный модуль (базово)
- ✅ Вход по паролю (заглушка admin)

---

## 🚧 Что в процессе / что осталось

### Фаза 1 — Быстрые улучшения (выполнено)
- Фото в протоколе (камера/галерея, подпись), вставка фото в PDF
- Импорт из JSON с отчётом об ошибках/конфликтах (админ → Импорт)
- Экспорт ZIP с медиа (админ → Экспорт → ZIP с медиа)

### Фаза 2 — Голосовой ввод
- Запись и загрузка аудио
- Облачное STT (реальный провайдер)
- Парсер полей из текста, автозаполнение формы
- Диалог уточнений, поиск пациентов голосом

### Фаза 3 — Админ и полировка
- Редактирование шаблонов протоколов
- Управление справочниками
- Настройки валидации

---

## 📋 Что нужно доделать (по плану MVP)

ID артефактов: [ARTIFACTS.md](ARTIFACTS.md). Коммиты привязывать к ID (например, `feat(VET-002): ...`).

### Фаза 1 — Быстрые улучшения и обмен данными
1. **VET-002** — Фото в протоколе и в PDF (UI привязки фото, хранение в examination_photos, вставка в PDF)
2. **VET-003** — Импорт из JSON (разбор, валидация, отчёт об ошибках/конфликтах)
3. **VET-004** — Экспорт ZIP с медиа (опционально) — полный бэкап с фото/аудио

### Фаза 2 — Голосовой ввод
4. **VET-005** — Запись и загрузка аудио
5. **VET-006** — Облачное STT, парсер полей, автозаполнение формы
6. **VET-007** — Диалог уточнений, поиск пациентов голосом

### Фаза 3 — Админ
7. **VET-008** — Редактирование шаблонов, справочники, настройки валидации

### После MVP
8. **VET-009** — Офлайн STT, приватный сервер STT, UI/UX, тесты

---

## 🏗️ Архитектура решения

### Структура модулей

```
lib/
├── core/
│   ├── config/          # Конфигурация приложения
│   ├── database/         # БД (Drift)
│   ├── di/              # Dependency Injection
│   └── utils/           # Утилиты
│
├── features/
│   ├── patients/        # Модуль пациентов
│   │   ├── data/        # Репозитории, дата-сорсы
│   │   ├── domain/       # Сущности, use cases
│   │   └── presentation/ # UI, страницы
│   │
│   ├── templates/       # Модуль шаблонов
│   │   ├── data/
│   │   ├── domain/
│   │   └── presentation/
│   │
│   ├── examinations/    # Модуль осмотров
│   │   ├── data/
│   │   ├── domain/
│   │   └── presentation/
│   │
│   ├── speech/          # Модуль распознавания речи
│   │   ├── domain/
│   │   │   ├── entities/      # SttResult, SttProvider
│   │   │   ├── interfaces/    # SpeechRecognizer
│   │   │   └── services/      # SttRouter
│   │   └── providers/         # Cloud, Private, OnDevice
│   │
│   ├── pdf/             # Модуль генерации PDF
│   │   ├── generators/
│   │   └── templates/
│   │
│   └── admin/           # Административный модуль
│       ├── data/
│       ├── domain/
│       └── presentation/
│
└── main.dart
```

### Поток данных при создании протокола

```
1. Выбор пациента
   ↓
2. Выбор типа протокола (шаблон)
   ↓
3. Запись/загрузка аудио
   ↓
4. SttRouter → выбор провайдера (облако/приватный/офлайн)
   ↓
5. Распознавание речи → текст + confidence
   ↓
6. Парсер полей → извлечение данных из текста
   ↓
7. Диалог уточнений (если нужно)
   ↓
8. Ручное редактирование (опционально)
   ↓
9. Валидация данных
   ↓
10. Сохранение в БД
   ↓
11. Генерация PDF
   ↓
12. Экспорт/шаринг
```

---

## 🔧 Технические детали

### Зависимости (основные)
- **Flutter** - фреймворк
- **Drift** - ORM для SQLite
- **go_router** - навигация
- **Riverpod** - управление состоянием
- **pdf** / **printing** - генерация PDF
- **record** / **flutter_sound** - запись аудио
- **http** / **dio** - HTTP клиент для облачного STT
- **get_it** - Dependency Injection

### База данных
- **SQLite** через Drift
- Версионирование схемы через миграции
- Индексы для оптимизации поиска

### Распознавание речи
- **Гибридный режим**: облако + приватный сервер + офлайн
- **Автоматический выбор** провайдера через SttRouter
- **Fallback** на офлайн при отсутствии интернета

---

## 📝 Следующие шаги

1. **VET-002** — Фото в протоколе и PDF (UI, сохранение путей, вставка в PDF)
2. **VET-003** — Импорт из JSON (загрузка бэкапа с валидацией и отчётом)
3. **VET-004** — Экспорт ZIP с медиа (опционально)
4. **VET-005**, **VET-006** — Запись/загрузка аудио, облачный STT, парсер полей, автозаполнение
5. **VET-008** — Админ: редактирование шаблонов, справочники, настройки валидации

---

## 🐛 Известные баги

- **VET-001** — [В PDF не отображаются русские символы](BUGS.md#vet-001) — исправлено (подключён шрифт Roboto с кириллицей).
- **VET-037** — [В админке по кнопке «Выход» не всегда попадаешь в список пациентов](BUGS.md#vet-037) — после выхода ожидается переход на список пациентов.

## 📚 Документация

- [Бизнес-требования](ТЗ_Бизнес_Требования.md)
- [Техническое задание](ТЗ_Техническое_Задание.md)
- [Реестр артефактов (ID для коммитов)](ARTIFACTS.md)
- [Известные баги](BUGS.md)
- [README](../README.md)

---

**Примечание**: Проект находится в активной разработке. Структура и архитектура могут изменяться по мере реализации функционала.
